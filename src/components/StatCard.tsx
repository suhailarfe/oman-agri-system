export function StatCard({ label, value, unit, icon: Icon, color, change }: { label: string; value: string; unit: string; icon: any; color: string; change: string }) {
  return <div className="relative overflow-hidden rounded-xl bg-white p-6 shadow-sm border border-gray-100 transition-all duration-200 hover:-translate-y-0.5 hover:shadow-md" style={{ borderRight: `3px solid ${color}` }}><div className="flex justify-between items-start mb-3"><span className="text-sm font-medium text-gray-500">{label}</span><div className="p-2 rounded-lg" style={{ background: `${color}15` }}><Icon size={16} color={color} /></div></div><div className="text-2xl font-bold" style={{ color: '#1a1a1a' }}>{value}</div><div className="flex items-center gap-2 mt-1"><span className="text-sm text-gray-400">{unit}</span><span className="text-xs font-medium" style={{ color }}>{change}</span></div></div>
}
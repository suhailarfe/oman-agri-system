# تقرير الفروقات وخطة الدمج الانتقائي مع نسخة Claude

## نطاق المراجعة

قورنت النسخة الخارجية المستخرجة في `/home/ubuntu/workspace/claude_completed_review` بالمشروع الحالي في `/home/ubuntu/oman-agri-vision-2040-v2`. يقتصر الدمج على الواجهة والتصميم. لا تُنسخ أي بنية خادم أو مسار أو طبقة بيانات من Claude.

## مقارنة الهيكل

| المنطقة | مشروع Claude | المشروع الحالي | قرار الدمج |
|---|---|---|---|
| الصفحة الرئيسية | `client/src/pages/Home.tsx` من 205 أسطر، بقصة تحريرية ومحتوى ثابت | `client/src/pages/Home.tsx` من 563 سطراً، مع بيانات مناطق ومصادقة وتحديثات مشرفين | دمج طبقات الواجهة التحريرية فقط حول البيانات الموجودة |
| صفحات التطبيق | `Home.tsx` و`NotFound.tsx` فقط | عشر صفحات، تشمل المناطق والجدوى ووثائق MVP وخارطة الطريق والإعدادات والملخص الأسبوعي | الإبقاء الكامل على المسارات الحالية |
| الخادم | `server/index.ts` يخدم ملفات ثابتة فقط | tRPC، OAuth، Drizzle، إشعارات، تخزين، وإجراءات محمية | عدم نقل أي كود خادم من Claude |
| قاعدة البيانات | لا يوجد `drizzle/` أو مخطط بيانات | تسعة ترحيلات وجداول للمناطق والوثائق والمراحل والتدقيق والإشعارات والتفضيلات | عدم تعديل المخطط أو الترحيلات |
| البيانات | مصفوفات مناطق وحلول ثابتة داخل `Home.tsx` | استعلامات tRPC للبيانات الحية | الإبقاء على استعلامات المشروع الحالي وعدم نسخ المصفوفات الثابتة |
| الاختبارات | لا يوجد أمر اختبار مخصص في `package.json` | Vitest و13 اختباراً قائماً | عدم إزالة أي اختبار وإضافة اختبار عند استخراج منطق جديد |

## عناصر Claude القابلة للاستفادة

| ملف Claude | العنصر الفريد | طريقة الاستفادة الآمنة |
|---|---|---|
| `client/src/pages/Home.tsx` | تسلسل قصصي محرر: «ملف الأرض»، مناطق، ماء، منظومة | إنشاء شرائط ومقاطع تحريرية داخل الصفحة الرئيسية الحالية مع إبقاء بيانات المناطق القادمة من tRPC |
| `client/src/pages/Home.tsx` | بطاقات مناطق هادئة ذات رقم وموجز للمحاصيل والمياه | إعادة صياغة الغلاف البصري لقسم الخريطة الحالي، مع روابط وصفحات المناطق الحالية |
| `client/src/pages/Home.tsx` | لوحة ماء ذات رسالة قصيرة وصورة أرض | إضافة قسم ماء تحريري يقرأ حلول الماء من بيانات المناطق الحالية بدلاً من بيانات ثابتة |
| `client/src/index.css` | إيقاع رأسي ومساحات تحريرية وخطوط فاصلة دقيقة | نسخ المبادئ فقط إلى فئات جديدة بأسماء مخصصة؛ عدم استبدال أوراق أنماط المشروع كاملة |

## عناصر لا تُدمج

لا تُدمج مصفوفات `regions` و`services` الثابتة، ولا `server/index.ts`، ولا `App.tsx` الخاص بـ Claude، ولا إعدادات الحزم. استبدال هذه الملفات سيزيل قاعدة البيانات والصلاحيات ومسارات المستثمرين والمشرفين والوثائق والتدقيق من المشروع الحالي.

## سجل الملفات والقرار التفصيلي

| ملف Claude | المقابل في المشروع الحالي | القرار | المبرر |
|---|---|---|---|
| `client/src/pages/Home.tsx` | `client/src/pages/Home.tsx` | اقتباس جزئي | نُقل الإيقاع التحريري لبطاقات المناطق، لكن البيانات والرابط يأتيان من `trpc.agri.getRegions` ومن المسار الحالي `/region/:code`. |
| `client/src/index.css` | `client/src/index.css` | اقتباس جزئي | أضيفت فئات `claude-ledger-*` فقط. لم تستبدل متغيرات الألوان أو أنماط صفحات النظام. |
| `client/src/App.tsx` | `client/src/App.tsx` | عدم دمج | مشروع Claude يقتصر على الصفحة الرئيسية و404. التطبيق الحالي يسجل مسارات الوثائق والخارطة والجدوى والإشعارات والتفاصيل. |
| `server/index.ts` | `server/_core/index.ts` و`server/routers.ts` | عدم دمج | خادم Claude يقدم تطبيقاً ثابتاً فقط، أما الخادم الحالي فيقدم OAuth وtRPC وقاعدة البيانات. |
| `package.json` | `package.json` | عدم دمج | نقل الحزم سيحذف اعتماديات MySQL وDrizzle وtRPC وVitest أو يعيد تعريف سكربتات التشغيل. |
| لا يوجد `drizzle/` | `drizzle/schema.ts` و`drizzle/0000..0008` | عدم دمج | مخطط البيانات والترحيلات في المشروع الحالي هما المرجع الوحيد للبيانات الدائمة. |
| لا توجد اختبارات | `client/src/lib/regionLedger.test.ts` | تحسين في المشروع الحالي | أضيف اختباران للتأكد من بقاء رابط بطاقات السجل داخل مسار المنطقة وترميز الرموز الخاصة. |

## ما تغير في المشروع الحالي

| الملف | التعديل المنفذ | أثره الوظيفي |
|---|---|---|
| `client/src/pages/Home.tsx` | إضافة قسم **ملف الأرض** بين المقدمة والخريطة، بخمس بطاقات تتغذى من نتائج المناطق المفلترة | يقدم مدخلاً تحريرياً جديداً لصفحات المناطق من دون إنشاء مسار موازٍ أو نسخ بيانات ثابتة |
| `client/src/index.css` | إضافة طبقة `claude-ledger-*` ذات حدود دقيقة وحالات مرور لونية فقط واستجابة هاتفية | يحافظ على هوية الفلج الحالية ويقتبس الإيقاع البصري دون ظلال أو حركة زائدة |
| `client/src/lib/regionLedger.ts` | توحيد توليد رابط تفاصيل المنطقة | يمنع تكرار صياغة الرابط ويضمن ترميز رمز المنطقة |
| `client/src/lib/regionLedger.test.ts` | اختبار الرابط العادي والرمز ذي المحرف الخاص | يحمي اتصال البطاقات الجديدة بالمسارات الحالية |
| `oman-agri-platform-builder/SKILL.md` | إضافة قاعدة الدمج الآمن لتصميم خارجي ثابت | يجعل المقارنة والتقييد على JSX وCSS وحفظ طبقة النظام إجراءً قابلاً لإعادة الاستخدام |

## تحقق التوافق

| مجال التحقق | النتيجة |
|---|---|
| مسارات التطبيق | فُحصت الصفحة الرئيسية و`/region/najd` و`/roadmap` و`/admin/docs` و`/feasibility` بصرياً، وعُرضت جميعها دون تضارب. |
| اختبارات Vitest | نجحت 9 ملفات اختبار و15 اختباراً، بما فيها اختبارا رابط السجل الجديد. |
| بناء الإنتاج | نجح `pnpm build`. توجد ملاحظة حجم حزمة Vite أكبر من 500 كيلوبايت، وهي تحذير أداء قائم وليست فشل بناء أو تعارضاً. |
| الصلاحيات والبيانات | لم يُغيّر `server/` أو `drizzle/` أو `App.tsx`. بقيت استدعاءات tRPC والمصادقة وإجراءات المشرف كما هي. |
| المهارة | اجتاز `oman-agri-platform-builder` التحقق السريع بعد إضافة إجراء الدمج الآمن. |

## خطة التنفيذ

1. أُضيف قسم **ملف الأرض** وطبقات المناطق التحريرية من دون استبدال بيانات المشروع أو طبقة الوصول.
2. بقيت المسارات والبيانات والتفاصيل المستقلة للمناطق كما هي.
3. لم يجر أي تعديل على `App.tsx` أو `server/` أو `drizzle/`.
4. اجتازت الاختبارات والبناء والفحص البصري للمسارات المحددة.
